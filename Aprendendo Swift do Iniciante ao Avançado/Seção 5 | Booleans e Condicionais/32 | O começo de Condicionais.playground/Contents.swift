// Booleans & Condicionais

var nome : String = "Pedro"

var maiorDeIdade : Bool = true

// Se for FALSE, o compilador vai ignorar o que está dentro do IF. Se for TRUE, o compilador executa o que está dentro do IF.

if maiorDeIdade {
    print("\(nome) pode tirar a carteira de motorista.")
}

print("\(nome) é um cara legal")
